/* Generated by _tools/estate_park.py from the map lab. Edit there. */

(function(){
var st=document.querySelector('.park-stage');
if(!st)return;

var NS='http://www.w3.org/2000/svg';

var map=st.querySelector('svg.park-map');
var hooks=[],timers=[],cleanups=[];
var STAR='M 0 -10 Q 1.4 -1.4 10 0 Q 1.4 1.4 0 10 Q -1.4 1.4 -10 0 Q -1.4 -1.4 0 -10 Z';
var INKS=['#FFFFFF','#FFF0DA','#B38A64','#679AA7','#AABEB3'];
var fx=null;
function layer(){if(!fx||!fx.isConnected){fx=document.createElementNS(NS,'g');fx.setAttribute('class','fx');map.appendChild(fx);}return fx;}
function mk(tag,attrs,parent){var e=document.createElementNS(NS,tag);for(var k in attrs)e.setAttribute(k,attrs[k]);(parent||layer()).appendChild(e);return e;}
function spark(x,y,o,parent){o=o||{};var s=o.s||(.4+Math.random()*.7),life=o.life||(900+Math.random()*700);
  var g=mk('g',{transform:'translate('+x.toFixed(1)+' '+y.toFixed(1)+')'},parent);
  var p=mk('path',{d:STAR,fill:o.fill||INKS[Math.random()*INKS.length|0],stroke:'#34372F','stroke-width':1.3,'class':'spk'},g);
  var dx=o.dx!=null?o.dx:(Math.random()-.5)*60,dy=o.dy!=null?o.dy:20+Math.random()*50;
  p.style.setProperty('--s',s);p.style.setProperty('--dx',dx+'px');p.style.setProperty('--dy',dy+'px');
  p.style.animationDuration=life+'ms';setTimeout(function(){g.remove();},life+50);return g;}
function burst(x,y,n,r,parent){for(var i=0;i<n;i++){var a=i/n*Math.PI*2+Math.random()*.3,d=(r||70)*(.6+Math.random()*.5);
  spark(x,y,{dx:Math.cos(a)*d,dy:Math.sin(a)*d,s:.5+Math.random()*.6,life:1000},parent);}}
function later(fn,ms){timers.push(setTimeout(fn,ms));}
function clearAll(){timers.forEach(clearTimeout);timers=[];cleanups.forEach(function(f){f();});cleanups=[];
  if(fx){fx.remove();fx=null;}map.querySelectorAll('.map-place.hit').forEach(function(a){a.classList.remove('hit');});}
function where(key){var h=map.querySelector('.map-'+key+' .map-halo');return {x:+h.getAttribute('cx'),y:+h.getAttribute('cy')};}
function hit(key,n){var a=map.querySelector('.map-'+key);if(!a)return;a.classList.remove('hit');void a.getBBox();
  a.classList.add('hit');var p=where(key);burst(p.x,p.y-50,n||14,100);}
var KEYS=['magnolia','valley','hall','deck','village','woods'];

window.LAB_PHOTOS={"village":{"title":"Overlook Village","blurb":"Four cottages along the hill, each turned to face out.","photos":["ov-2","ov-4","ov-8"]},"magnolia":{"title":"Magnolia House","blurb":"Columns, glass, and the first photograph every guest takes.","photos":["mh-2","mh-3","mh-6"]},"lodge":{"title":"The Lodge","blurb":"The largest party sleeps here, and the morning starts here.","photos":["ov-5","ov-6","ov-7"]},"hall":{"title":"Davis Hall","blurb":"Dinner, the first dance, and the largest floor on the estate.","photos":["dh-2","dh-3","dh-5"]},"deck":{"title":"The Lookout Deck","blurb":"Fifteen thousand square feet out over the valley.","photos":["ld-2","ld-4","ld-7"]},"valley":{"title":"The Valley","blurb":"The meadow, held on three sides, open to the mountain.","photos":["tv-2","tv-3","tv-7"]},"woods":{"title":"Lost in the Woods","blurb":"One cabin at the far edge, for the two of you.","photos":["close-woods","sd-fire","stay-inside"]}};
function peekInit(opts){
  opts = opts || {};
  var stage = document.querySelector('.park-stage');
  var svg = stage.querySelector('svg');
  var peek = document.createElement('div');
  peek.className = 'peek';
  peek.innerHTML = '<figure><img alt=""><figcaption><b></b><span></span></figcaption></figure>';
  stage.appendChild(peek);
  var img = peek.querySelector('img'), ttl = peek.querySelector('b'), sub = peek.querySelector('span');
  var shots = window.LAB_PHOTOS, timer = null, at = 0, cur = null;

  function place(el){
    // Anchor to the building, not the pointer, so it never jitters. A place
    // near the top of the drawing -- the village sits hard against it -- has
    // no room for the photograph above it, so the panel hangs below instead,
    // and it is kept inside the drawing left and right.
    // measured on the drawing itself: the place's halo is far taller than
    // what is on the page, and would send every panel downward
    var art = el.querySelector('.map-art') || el;
    var r = art.getBoundingClientRect(), s = stage.getBoundingClientRect();
    var w = peek.offsetWidth || 260, h = peek.offsetHeight || 240;
    var down = (r.top - h - 10) < 8;
    peek.classList.toggle('down', down);
    var x = r.left - s.left + r.width / 2;
    x = Math.max(w / 2 + 8, Math.min(x, s.width - w / 2 - 8));
    peek.style.left = x + 'px';
    peek.style.top  = ((down ? r.bottom : r.top) - s.top) + 'px';
  }
  function show(el){
    var key = el.getAttribute('data-place');
    if (!shots[key]) return;
    cur = key; at = 0;
    img.src = '/assets/img/' + shots[key].photos[0] + '.webp';
    img.alt = shots[key].title;
    ttl.textContent = shots[key].title;
    sub.textContent = shots[key].blurb;
    place(el); peek.classList.add('on');
    clearInterval(timer);
    // The place has eight frames; three of them is enough to say "there is
    // more of this" without turning the map into a slideshow.
    timer = setInterval(function(){
      at = (at + 1) % shots[cur].photos.length;
      img.src = '/assets/img/' + shots[cur].photos[at] + '.webp';
    }, 1500);
  }
  function hide(){ peek.classList.remove('on'); clearInterval(timer); cur = null; }

  svg.querySelectorAll('.map-place').forEach(function(el){
    el.addEventListener('mouseenter', function(){ show(el); });
    el.addEventListener('focus', function(){ show(el); });
    el.addEventListener('mouseleave', hide);
    el.addEventListener('blur', hide);
  });
  stage.addEventListener('mouseleave', hide);
  if (opts.after) opts.after(svg, stage);
}

peekInit();
// reach for a place and it throws a little sparkle
var lastHover={};
map.querySelectorAll('.map-place').forEach(function(a){
  a.addEventListener('mouseenter',function(){var k=a.getAttribute('data-place'),now=Date.now();
    if(now-(lastHover[k]||0)<700)return;lastHover[k]=now;var p=where(k);burst(p.x,p.y-60,10,80);});
});

var TRAIL='M 60 820 C 120 560 380 420 620 360 C 880 300 900 640 1180 610 C 1460 580 1380 200 1680 170 C 1880 150 1980 300 2120 250';
var world=document.createElementNS(NS,'g');world.setAttribute('class','world');
Array.prototype.slice.call(map.childNodes).forEach(function(n){if(n.nodeName!=='defs')world.appendChild(n);});
map.appendChild(world);
var defs=map.querySelector('defs'),mask=null,again=st.querySelector('.park-again');

// Hidden under the mask until the wand reaches it. If the intro cannot run
// for any reason the mask is never added, so the map simply shows.
function cover(){
  mask=mk('mask',{id:'dust',maskUnits:'userSpaceOnUse',x:-300,y:-300,width:2600,height:1400},defs);
  mk('rect',{x:-300,y:-300,width:2600,height:1400,fill:'#fff','fill-opacity':0,'class':'dust-base'},mask);
  mk('path',{d:TRAIL,fill:'none',stroke:'#fff','stroke-width':520,'stroke-linecap':'round',pathLength:1,
    'stroke-dasharray':'1 1','stroke-dashoffset':1,'class':'dust-trail'},mask);
  world.setAttribute('mask','url(#dust)');
}
function uncover(){world.removeAttribute('mask');if(mask){mask.remove();mask=null;}}

var raf=0;
function play(){
  clearAll();cancelAnimationFrame(raf);uncover();cover();
  if(again)again.hidden=true;
  var base=mask.querySelector('.dust-base'),p=mask.querySelector('.dust-trail');
  var guide=mk('path',{d:TRAIL,fill:'none'},defs);
  var head=mk('g',{'class':'wand'});
  mk('circle',{r:30,fill:'#FFF0DA',opacity:.8},head);
  mk('path',{d:STAR,fill:'#fff',stroke:'#34372F','stroke-width':1.2,transform:'scale(2.2)'},head);
  var L=guide.getTotalLength(),t0=null,D=3200,done={};
  var spots=KEYS.map(function(k){var q=where(k);return {k:k,x:q.x,y:q.y};});
  function finish(){
    base.style.transition='fill-opacity .7s ease';base.style.fillOpacity=1;
    spots.forEach(function(s){if(!done[s.k]){done[s.k]=1;hit(s.k);}});
    later(function(){uncover();guide.remove();if(again)again.hidden=false;},800);
  }
  function step(now){
    if(t0===null)t0=now;
    var k=Math.min(1,(now-t0)/D),e=k<.5?2*k*k:1-Math.pow(-2*k+2,2)/2;
    p.setAttribute('stroke-dashoffset',(1-e).toFixed(4));
    var pt=guide.getPointAtLength(e*L);
    head.setAttribute('transform','translate('+pt.x.toFixed(1)+' '+pt.y.toFixed(1)+')');
    for(var i=0;i<3;i++)spark(pt.x+(Math.random()-.5)*50,pt.y+(Math.random()-.5)*50,{dy:30+Math.random()*80,life:1300});
    spots.forEach(function(s){if(!done[s.k]&&Math.hypot(pt.x-s.x,pt.y-s.y)<300){done[s.k]=1;hit(s.k,18);}});
    if(k<1)raf=requestAnimationFrame(step);else{head.remove();finish();}
  }
  raf=requestAnimationFrame(step);
}
if(again)again.addEventListener('click',play);

// On a phone the drawing is wider than the screen; start it on the middle of
// the estate rather than its left edge.
var sc=st.querySelector('.park-scroll');
if(sc&&sc.scrollWidth>sc.clientWidth)sc.scrollLeft=(sc.scrollWidth-sc.clientWidth)*.42;

if('IntersectionObserver' in window){
  var r=st.getBoundingClientRect();
  // Hidden in advance unless it is already on screen; in the hero it always
  // is, and the stylesheet has kept it hidden until now.
  if(r.top>innerHeight*.9||st.classList.contains('park-hero'))cover();
  st.classList.add('ready');
  var io=new IntersectionObserver(function(es){
    if(es[0].intersectionRatio>=.35){io.disconnect();play();}
  },{threshold:[0,.35,.6]});
  io.observe(st);
}else{st.classList.add('ready');if(again)again.hidden=false;}
})();
